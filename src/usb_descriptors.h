#ifndef USB_DESCRIPTORS_H_
#define USB_DESCRIPTORS_H_

// Don't think this is needed until reports
enum
{
  REPORT_ID_KEYBOARD = 1,
  REPORT_ID_MOUSE,
  REPORT_ID_CONSUMER_CONTROL,
  REPORT_ID_GAMEPAD,
  REPORT_ID_COUNT
};

#endif
